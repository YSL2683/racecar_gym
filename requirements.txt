# racecar_gym 핵심 패키지는 먼저 아래 명령으로 설치하세요:
#   pip install -e .
#
# 이후 아래 명령으로 나머지 의존성을 설치하세요:
#   pip install -r requirements.txt

# ─── racecar_gym 핵심 의존성 ────────────────────────────────────────────────────
pybullet==3.2.5
scipy
numpy==1.22.3
gymnasium==0.28.1
yamldataclassconfig==1.5.0
nptyping<2.0
pettingzoo==1.22.3

# ─── 시각화 ────────────────────────────────────────────────────────
opencv-python>=4.8

# ─── 유틸리티 ──────────────────────────────────────────────────────────────────
tqdm>=4.65
matplotlib>=3.5

# ─── 강화학습 ─────────────────────────────────────────────────────────────────
stable-baselines3[extra]>=2.0
# numpy constraints:
#   >= 1.22.4  required by pandas 2.x (pulled in by SB3[extra])
#   <  1.24    required by nptyping<2.0 and racecar_gym/bullet/util.py (use np.float)
numpy==1.23.5

